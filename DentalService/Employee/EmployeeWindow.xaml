<Window x:Class="DentalService.Employee.EmployeeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DentalService.Employee"
        mc:Ignorable="d"
        Title="EmployeeWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border BorderThickness="1" BorderBrush="Black" Background="DimGray">
            <StackPanel Orientation="Horizontal" Margin="16 8 16 8" Height="32">
                <Button x:Name="DentisScheduleButton" Width="120" Background="White" Click="DentisScheduleButton_Click" Content="Dentis Schedule" />
                <Button x:Name="CustomerButton"  Width="120" Background="White" Click="CustomerButton_Click" Margin="16 0 0 0" Content="Customer"/>
                <Button x:Name="a"  Width="120" Background="White" Click="CustomerButton_Click" Margin="16 0 0 0" Content="Customer"/>
            </StackPanel>
        </Border>
        
        <Frame x:Name="Content" Grid.Row="1" Navigated="Content_Navigated"/>
    </Grid>
</Window>
